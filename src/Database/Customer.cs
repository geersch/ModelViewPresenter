namespace Database
{
    public partial class Customer 
    {
        // Extend the partial Customer class here. The Entity Data Model already contains
        // an automatically generated partial Customer class. To prevent your custom additions
        // from being erased when regenerating the model put the code is a separate class file.
    }
}
